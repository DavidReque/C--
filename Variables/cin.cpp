#include <iostream>

using namespace std;

int main()
{
    // Declaración de una variable entera llamada miVariable de tipo entero
    int miVariable;

    // Mostrar un mensaje en la consola pidiendo al usuario que ingrese un valor
    cout << "Ingrese variables:" << endl;

    // Leer un valor desde la consola y almacenarlo en la variable miVariable
    cin >> miVariable;

    // Mostrar el valor almacenado en la variable miVariable
    cout << "La variable guardada es: " << miVariable;
    
    return 0;
}
